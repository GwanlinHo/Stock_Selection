#  選股系統開發紀錄 (Development Log)

## [2026-04-20] 系統效能強化與格式重構 (重大更新)

### 1. 篩選門檻收緊 (Filter Tightening)
*   **優化目標**: 提升選股含金量，並大幅減少 API 配額消耗。
*   **變更內容**:
    *   **L2 成交量門檻**: 由原本的 1 張 (1,000 股) 提高至 **1,000 張 (1,000,000 股)**，有效排除低流動性殭屍股。
    *   **L1 月線斜率門檻**: 將 MA20 斜率要求由 0.1% 提高至 **0.5%**，確保標的具備明確上升趨勢。
    *   **結果**: 海選入圍數量由 600+ 檔大幅下降至 ~250 檔，最終精選池維持在 **50 檔以內**。

### 2. 斷點續傳機制 (Resumable Execution)
*   **實作內容**: 
    *   精煉階段（L3/L4）啟動前會主動偵測 `candidates.json` 進度。
    *   自動跳過今日已成功抓取數據的標的，節省 FinMind API 配額。
    *   增加「每處理 10 檔自動存檔」機制，確保程序中斷時資料不流失。

### 3. 數據品質與邏輯修正
*   **營收數據 (YoY)**: 將營收抓取天數由 120 天擴大至 **450 天**，確保滿足 L4 計算 YoY（年增率）所需的 13 個月對照期，解決了營收數據顯示為 0 的問題。
*   **清理機制優化**: 
    *   快取清理改用檔案修改時間 (`mtime`) 判斷。
    *   新增 **週報自動清理**: 自動移除超過 30 天的舊 Markdown 報告，保持資料夾整潔。

### 4. 報告格式統一 (Standardized Format)
*   **佈局調整**:
    1.  **AI 深度分析** 直接置頂，提供即時市場洞察。
    2.  新增 **篩選漏斗統計**: 透明化呈現各階段（價量 -> 籌碼 -> 營收）過濾出的標的數量。
    3.  **精選池表格收緊**: 僅列出「全關卡通過 (L4)」的標的，不再顯示未達標之候選股。

---

## [2026-04-19] 核心功能完備
*   完備週報數據顯示（法人張數、營收 YoY%）。
*   修正 Metadata 對照邏輯，確保產業名稱正確顯示。
*   優化日線 MA 計算邏輯。

## [2026-04-22] 深度基本面指標與 AI 去罐頭化架構優化
### 新增功能
- **深度基本面指標整合**：成功導入 ROE (TTM 年化)、PER、PEG 指標至 L4 精煉階段。
- **ROE 計算邏輯優化**：由單季簡易 ROE 改為 TTM (Trailing Twelve Months) 近四季加總年化 ROE，有效反映獲利品質。
- **AI 自動評等系統**：整合 Gemini API，針對通過 L4 的標的自動進行 A/B/C 分類與核心分析。
- **報告結構升級**：週報新增「篩選標準定義」區塊，並根據 AI 評等進行分組呈現。

### 架構優化
- **去罐頭化分析設計**：確立「數據驅動 (Python) + 趨勢洞察 (Agent Web Search)」的協作模式。
- **頻率限制防護**：優化數據抓取間隔 (Sleep 1.2s)，降低 FinMind API 連線失敗率。

### 篩選門檻調整
- L4 營收門檻：YoY > 5%。
- L4 品質門檻：ROE (TTM) > 8%。
- PEG 指標目前採呈現但不硬性過濾模式，保留人工判斷彈性。

## [2026-04-22] 系統架構重歸設計初衷：Agent 分析與數據持久化
### 重大變更
- **移除 API Key 依賴**：刪除 `src/ai_analyzer.py`，移除 `main.py` 中所有依賴外部 API Key 的自動化分析程式碼。
- **實作「數據軟失效 (Soft Fail)」機制**：在精煉階段，若 API 抓取失敗，系統將自動從 `candidates.json` 恢復舊數據，防止因頻率限制導致數據歸零或被覆寫。
- **重新定義 AI 分析權責**：確立 Python 程式僅負責「數據獲取與篩選」，而「深度質化分析」完全由 Gemini CLI 透過 `google_web_search` 執行並手動寫入報告，確保去罐頭化。

### 數據策略
- **舊數據價值**：考量中長期投資，舊數據（如前季 ROE）仍具備 80% 以上的參考價值。系統現在會自動「縫合」新舊數據，確保精選池的穩定性。
